# 2x2 매트릭스 빌더

브라우저에서 바로 쓰는 2x2 매트릭스(사분면) 빌더입니다. 빌드나 설치 없이 `index.html` 파일 하나로 동작합니다.

`main` 브랜치에 푸시될 때마다 GitHub Actions(`.github/workflows/pages.yml`)가 저장소를 GitHub Pages로 자동 배포합니다. (저장소 Settings → Pages에서 소스가 "GitHub Actions"로 설정되어 있어야 합니다.)

> 공익활동 회고용 저널링 프롬프트 캔버스는 별도 저장소로 옮겼습니다 → [h2dj/Jpromptcanvas](https://github.com/h2dj/Jpromptcanvas) ([h2dj.github.io/Jpromptcanvas](https://h2dj.github.io/Jpromptcanvas))

## 사용법

- `index.html`을 브라우저로 열면 바로 시작할 수 있습니다.
- 또는 GitHub Pages 등 정적 호스팅에 그대로 올려서 사용할 수 있습니다.

## 기능

- **템플릿으로 시작하기**: 자주 쓰는 2x2 프레임워크 11종을 선택하면 축 라벨·사분면·예시 항목이 즉시 채워짐 (아래 목록 참고)
- **제목 / 축 라벨 편집**: 매트릭스 제목, 가로축(X)·세로축(Y) 이름과 양 끝 라벨을 자유롭게 수정
- **사분면 커스터마이즈**: 4개 사분면의 이름과 배경색을 각각 지정
- **항목 추가/편집/삭제**: 텍스트와 색상을 지정해 항목(카드)을 추가하고, 매트릭스 위에서 드래그로 자유롭게 배치
- **항목 선택 & 키보드 단축키**: 매트릭스 위 항목(또는 사이드바 목록 행)을 클릭해 선택 후, `Ctrl`/`Cmd`+`D`로 같은 텍스트·색상의 항목을 복제하거나 `Insert`(맥 키보드는 `Tab`)로 같은 사분면·같은 색의 새 항목을 추가. `Esc` 또는 매트릭스 빈 곳 클릭으로 선택 해제
- **항목 정렬 & CSV 내보내기**: 사이드바 항목 목록을 X축·Y축 이름 버튼으로 정렬(클릭 시 오름차순/내림차순 전환)하고, 현재 정렬 순서 그대로 CSV로 내보내기
- **자동 저장**: 모든 변경 사항은 브라우저 `localStorage`에 자동 저장되어 새로고침해도 유지
- **초기화**: 기본 예시(아이젠하워 매트릭스)로 되돌리기
- **JSON 내보내기 / 가져오기**: 작업 내용을 JSON 파일로 저장하고 다시 불러오기
- **이미지로 저장**: 완성한 매트릭스를 PNG 이미지로 내보내기

기본값은 "긴급도 vs 중요도" 아이젠하워 매트릭스 예시로 채워져 있으며, 모든 라벨/항목을 자유롭게 바꿔 임의의 2x2 프레임워크로 활용할 수 있습니다.

### 내장 템플릿

**업무/생산성**
- 아이젠하워 매트릭스 (긴급도 × 중요도)
- 노력-효과 매트릭스 (실행 노력 × 기대 효과)
- 위임 매트릭스 (내 역량 × 업무 중요도)

**전략/기획**
- BCG 매트릭스 (시장 점유율 × 시장 성장률)
- Ansoff 매트릭스 (제품 × 시장)
- TOWS 매트릭스 (강점·약점 × 기회·위협)

**아이디어 평가**
- 가치-실현가능성 매트릭스 (사용자 가치 × 구현 용이성)
- 비용-가치 매트릭스 (백로그 우선순위)

**개인 성찰/의사결정**
- 감정-에너지 매트릭스 (감정 체크인)
- 관심-영향력 매트릭스 (이해관계자 분석)

**학습/역량 개발**
- 숙련도-중요도 매트릭스 (디지털 역량 자가진단)
